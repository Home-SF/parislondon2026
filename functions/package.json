{
  "name": "trip-daily-email",
  "description": "Scheduled function that emails the day's agenda to trip participants at 8 AM local time",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "lint": "echo 'no lint configured — skipping' && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
